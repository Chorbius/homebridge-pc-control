{
  "pluginAlias": "PCControl",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "My PC"
      },
      "macAddress": {
        "title": "MAC Address",
        "type": "string",
        "required": true,
        "format": "mac",
        "description": "The MAC address of your PC's network interface"
      },
      "ipAddress": {
        "title": "IP Address",
        "type": "string",
        "required": true,
        "format": "ipv4",
        "description": "The IP address of your PC"
      },
      "port": {
        "title": "Port",
        "type": "integer",
        "required": true,
        "default": 3000,
        "description": "The port number your PC's control server is listening on"
      }
    }
  }
}